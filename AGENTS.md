# 工程协作约定

以下说明适用于整个仓库。保留更严格的父级或嵌套说明。

## 实现前

- 在确定方案前，检查架构、相邻代码、测试、格式化、代码检查、类型检查、CI 和文档。
- 对非简单或存在歧义的工作使用 `$shape-requirements`，明确范围、非目标、验收标准、边界情况和验证方式。
- 优先遵循仓库已有模式。保持改动聚焦，并保留用户不相关的工作。

## 实现期间

- 编辑过程中使用最小但有效的反馈闭环进行验证。
- 将身份认证、权限校验、输入、密钥、敏感数据、持久化、依赖和部署视为安全敏感内容。
- 对于长任务，在阶段边界使用 `$preserve-task-context`，并按照仓库文档约定维护精简的当前状态交接记录。

## 项目命令

- 开发：`npm run dev`
- 格式与代码检查：`npm run lint`
- 类型检查：`npm run typecheck`
- 单元测试：`npm test`
- 构建：`npm run build`
- Chromium 端到端测试：`npm run e2e`

## 完成门禁

- 修改任何源码、配置、数据结构、依赖、UI、测试、文档或流水线后，在完成前使用 `$finish-engineering-change`。
- 始终运行相关定向测试，以及已经配置的格式化、代码检查、类型和构建检查。
- 根据实际风险补充集成、端到端、负向、无障碍、安全、迁移或更广泛的回归检查。
- 可见 UI 使用 `$audit-ui-consistency`；信任边界改动使用 `$review-application-security`；已记录行为变化使用 `$maintain-project-docs`；交付改动使用 `$harden-cicd`。
- 报告准确命令和结果，包括跳过、受阻、不可用、偶发和预先存在的失败。

## 安全

- 不得暴露密钥或生产数据。
- 未经明确授权，不得部署到生产环境，也不得执行破坏性或不可逆的外部操作。
- 最终交接必须覆盖行为变化、重要文件、验证证据、文档影响和残余风险。
